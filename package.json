{
  "name": "nexustech-site",
  "version": "1.0.0",
  "description": "NexusTech static site - production build scripts",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "start": "npx serve dist -s"
  },
  "devDependencies": {
    "axios": "^1.6.2",
    "fs-extra": "^11.1.1",
    "html-minifier-terser": "^7.0.0",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6"
  }
}
